package ru.littleligr.magic.engine.spell.target;

import net.minecraft.util.math.BlockPos;

public class BlockPosTarget extends Target<BlockPos> {
    public BlockPosTarget(BlockPos target) {
        super(target);
    }
}
